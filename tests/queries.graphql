query fetchVersion {
  myRequiredString
  myOptionalInt

  myType {
    foo
    nested {
      bar
    }
  }
}

query multiply($a: Int!, $b: Float!, $printResult: Boolean) {
  multiply(a: $a, b: $b, printResult: $printResult)
}
