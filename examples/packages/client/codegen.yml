schema: http://localhost:4000
documents: src/queries/demo.graphql
overwrite: true
generates:
  ./src/queries/demo.graphql.ts:
    # Using an absolute path here because graphql-code-generator won't ../ in paths
    - apollo-hooks-codegen
